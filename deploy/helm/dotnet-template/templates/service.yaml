apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-template.fullname" . }}
  labels:
    {{- include "dotnet-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "dotnet-template.selectorLabels" . | nindent 4 }}
